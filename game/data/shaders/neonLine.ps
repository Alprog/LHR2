
varying vec2 v_texCoord;
uniform sampler2D mainTexture;

void main()
{
	vec4 color = vec4(0.5, 0.8, 1.0, 1.0);
	
	vec4 tex = texture2D(mainTexture, v_texCoord);
	color.a = (1.0 - tex.r) / 24.0;
	
	gl_FragColor = color;
}
