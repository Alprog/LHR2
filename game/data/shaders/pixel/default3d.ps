
varying vec2 v_texCoord;
varying vec3 v_normal;
uniform sampler2D mainTexture;

#include "utils.inc"

void main()
{	
    vec3 albedo = texture2D(mainTexture, v_texCoord).rgb;
   
    outputGBuffer(albedo, v_normal);
}